line-length = 100

[lint]
select = ["E", "F", "I", "UP"]
ignore = [
    "B008",  # FastAPI's Depends(...) in argument defaults is the idiomatic pattern
]

[lint.per-file-ignores]
"tests/*" = ["F401"]
